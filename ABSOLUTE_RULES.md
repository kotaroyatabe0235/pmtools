このルールは、いかなる状況においても、必ず守られるべきルールで逸脱は許されない。また、このルールの変更は人間によってのみ行われるため、いかなる場合も変更は許容されない。
このファイルを参照したら、すぐさま絶対順守ルールとして自身のメモリに必ず保存する。

# まずやること
* 本ディレクトリ配下のmdファイルを確認しろ
* 本ディレクトリはgitリポジトリの状態を確認しろ

# 作業進行上の絶対ルール
* 推測する代わりに前提を述べよ
* 外科手術を執り行うように作業せよ  

# ルール
* 変更はmainブランチからfeatureブランチを作成し、人間に変更のPR を承認させる
* PRは、レビューコメントがつけられた場合は、対応して欲しい旨の指示を出す。受け取ったらGithubCLIを活用して指摘を確認し対応し、各コメントに返信した上で、コミットpushせよ
* featureの単位は、50〜100stepを上限とし、意味ある単位に計画する
* 作業の状況は逐一標準出力するとともに、コードはコードと同量のコメントで実装の意図や背景を記載する

# 案件
## 案件概要
* microsoft projectを実装する

## 技術要素
* Spring Boot + thimeleaf + jQuery
* postgreSQL
* composeでコンテナとして稼働
上記以外の技術要素の採用は認めない。

## 開発スタイル
* TDD

## 計画
* 設計
* 必要ルールの洗い出しと人間による定義
* テスト製造
* 実装
* READMEの作成
全体において、各フェーズの最初と最後に、計画・または再計画を行う。

## ディレクトリ
root
>docs            : design documentを配置するディレクトリ。この配下にmd形式でドキュメントを配置。これ以上のディレクトリ作成は許可しない。
>rules           : 各種ルールを配置するディレクトリ（配下ファイルは人間のみ変更を許可）。ルールはmd形式で人間が配置。これ以上のディレクトリ作成は許可しない。
>src             : 実態ソースを配置するディレクトリ。自由にファイルを配置して良い。内部のディレクトリ作成も任意。
>tools           : dockerfileやCIに関する設定ファイル等を配置するディレクトリ。内部のディレクトリ作成も任意。
>README.md       : 単一のmdファイル。使用や使い方などを最終的に取りまとめる。
>ABSOLUTE_RULE.md: 開発における絶対的なルールやこのリポジトリの目的などを記載する。(人間のみ編集可能)

## 追加ルール

### 追加ルール 1: プレビューコメント取得と適用
GitHub の PR プレビューコメントの取得と対応フローを確立する。

**手順:**
1. PR 番号を特定
2. GitHub CLI でレビューコメントを取得:
   ```bash
   gh pr view <PR_NUMBER> --json reviews,comments --limit 1000
   ```
3. コメント内容を解析:
   - **Review Threads**: 議論の要約
   - **Review Comments**: インライン指摘
   - **General Comments**: PR 本文のコメント
   - **Review Summary**: PR の要約
4. AI に共有し、対応指示を出させる:
   ```
   以下の PR レビューコメントを確認して対応して:
   <ここにコメント内容貼り付け>
   ```
5. 修正コミットを PR にパッチ適用:
   ```bash
   gh pr submit --repo=<OWNER>\ --repo=<REPO> --rebase
   ```

**注意事項:**
- プレビューコメントを取得できない場合は、gh api repos/<OWNER>/<REPO>/pulls/<PR_NUMBER>/comments で raw JSON を取得
- コメントは UTF-8 エンコーディングで取得
- 各指摘は「修正する」「議論保留」「同意」いずれかとして分類
- 修正後の差分レビューも取得し、再チェックを行う
>PLAN.md         : 開発の計画を記載する
